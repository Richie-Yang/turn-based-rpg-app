export enum NodeEnv {
  LOCAL = 'local',
  DEV = 'dev',
  PROD = 'prod',
}

export enum Models {
  USER = 'user',
  TODO = 'todo',
  TODO_LIST = 'todoList',
  CHARACTER_BASE = 'characterBase',
  CHARACTER = 'character',
  TEAM = 'team',
}

export enum Roles {
  ADMIN = 'admin',
  USER = 'user',
}
